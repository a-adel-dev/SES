
public enum ClassroomState { 
    inSession,
    onBreak,
    dayIsOver,
    simIsOver
}

public enum ActivityType { 
    Breathing,
    Talking,
    LoudTalking
}

public enum MaskFactor { 
    none,
    cloth,
    surgical,
    N95
}

public enum AIStatus { 
    inClass,
    inLab 
}